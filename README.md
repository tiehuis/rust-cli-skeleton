A sample project skeleton for a command-line application in Rust.

## Installation

Instead of `cargo new --bin myapp` run the following

```
git clone https://tiehuis/rust-cli-skeleton myapp
cd myapp
# Modify Cargo.toml with your details
```
